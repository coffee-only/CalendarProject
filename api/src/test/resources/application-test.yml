spring:
  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: validate



#debug: true
logging:
  level:
    org.springframework:
      #data: DEBUG
      security: TRACE
      #hibernate:
      #  orm:
      #    model:
      #      bind: NONE
